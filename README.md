<h1>Carteira na Mão 💸</h1>

<h3>🌎 Sobre o Projeto</h3>

O **Carteira na Mão** nasceu da necessidade de unificar o controle financeiro do dia a dia (contas a pagar, receitas, despesas) com o gerenciamento avançado de uma carteira de investimentos (ações, FIIs, renda fixa).

A plataforma é construída com uma API robusta e segura, onde cada usuário tem posse total e privada de seus dados, garantindo que nenhuma informação financeira seja misturada.


### Criar ambiente virtual

#### 🍏 MacOS & 🐧 Linux (Ubuntu/Debian/Fedora/etc.)
````bash
python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

#Iniciando servidor
python3 manage.py
````

#### 🪟 Windows
````bash
python -m venv venv && venv\Scripts\activate && pip install -r requirements.txt

#Iniciando servidor
python manage.py
````